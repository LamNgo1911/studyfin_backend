---
phase: quick/lz4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/modules/sync/sync.service.ts
  - src/modules/sync/sync.spec.ts
  - src/modules/programs/programs.service.ts
  - src/modules/programs/programs.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sync service extracts hakukohteet and duration from upstream koulutus detail during upsertProgram"
    - "GET /programs/:oid returns hakukohteet and duration fields in the response"
    - "Programs with no application targets return null for hakukohteet (not an empty array or crash)"
    - "Existing programs in DB (where hakukohteet/duration are null) serve detail without error"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "hakukohteet (Json?) and duration (String?) columns on Program model"
      contains: "hakukohteet"
    - path: "src/modules/sync/sync.service.ts"
      provides: "resolveHakukohteet() helper for extracting and resolving upstream hakukohteet to English"
      exports: ["resolveHakukohteet (private method)"]
      pattern: "hakukohteet.*Prisma\\.(InputJsonValue|JsonNull)"
    - path: "src/modules/programs/programs.service.ts"
      provides: "hakukohteet and duration fields in mapProgramDetail() response"
      pattern: "hakukohteet"
  key_links:
    - from: "sync.service.ts :: upsertProgram()"
      to: "prisma.program.upsert :: create/update"
      via: "hakukohteet and duration passed to upsert payload"
    - from: "programs.service.ts :: mapProgramDetail()"
      to: "row.hakukohteet / row.duration"
      via: "direct property access on Prisma result row"
---

<objective>
Add requirements and admissions info (hakukohteet / application targets) to program details.

Purpose: Program detail pages currently lack application deadline, prerequisite education requirements, and admission criteria information. This data exists in the upstream Opintopolku API's koulutus detail response under the `hakukohteet` field (application targets) but is never extracted or stored. Additionally, `kesto` (duration) is not stored either, though it is available upstream.

Output:
- Program model gains `hakukohteet` (Json?) and `duration` (String?) columns
- Sync service extracts, English-resolves, and stores hakukohteet during upsertProgram
- Programs service exposes hakukohteet and duration in GET /programs/:oid detail response
- All existing tests pass; new test coverage for the extraction and exposure
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/references/mandatory-read.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- Key types and contracts executor needs. Extracted from codebase. -->

From prisma/schema.prisma (lines 51-80):
- Program model currently has fields: id, oid, name, description, type, typePath, isDegree, imageUrl, creditsAmount, creditsUnit, eqfLevel, nqfLevel, fieldOfStudy, degreeTitles, teachingLanguages, implementations (Json?), syncedAt, createdAt, updatedAt
- New fields to add: hakukohteet (Json?), duration (String?)

From sync.service.ts :: resolveImplementations() (lines 368-384):
- Pattern for resolving complex nested JSON data from upstream and returning Prisma.InputJsonValue | typeof Prisma.JsonNull
- Follows this shape: filter, map to cleaned objects, return array or Prisma.JsonNull

From sync.service.ts :: upsertProgram() (lines 230-366):
- Extracts detail fields from upstream response, creates resolveLang closure
- Upserts via prisma.program.upsert() with create and update blocks
- Uses detail.toteutukset, detail.kielivalinta, detail.metadata.*, detail.tarjoajat
- detail.hakukohteet and detail.kesto are available upstream but not currently used

From programs.service.ts :: mapProgramDetail() (lines 114-139):
- Returns an object with all program detail fields
- Pattern: row.fieldName ?? null for nullable fields, row.fieldName ?? [] for array fields
- Client expects null (not empty array) for nullable JSON fields — see line 130: `implementations: row.implementations ?? null`

From programs.spec.ts (lines 7-30):
- mockProgramRow and mockProgramDetailRow define test fixtures
- New tests should verify hakukohteet and duration appear when present
- Existing assertions must not break (null fields should be tolerated)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hakukohteet and duration to Prisma schema and sync extraction</name>
  <files>
    prisma/schema.prisma
    src/modules/sync/sync.service.ts
    src/modules/sync/sync.spec.ts
  </files>
  <action>
    1. In prisma/schema.prisma, add two fields to the Program model (after line 67, near existing `implementations Json?` field):
       - `hakukohteet   Json?       // resolved hakukohteet (application targets)`
       - `duration      String?     // kesto from upstream (e.g. "3 years" or "5 years")`

    2. In src/modules/sync/sync.service.ts, add a private method `resolveHakukohteet()` following the same pattern as `resolveImplementations()`:
       - Signature: `private resolveHakukohteet(hakukohteet: any[] | null | undefined, resolveLang: (obj: any) => string): Prisma.InputJsonValue | typeof Prisma.JsonNull`
       - Guard clause: if not Array.isArray or empty, return Prisma.JsonNull
       - Only include hakukohteet linked to English-teaching implementations. Filter by checking if the hakukohde has a `kaytetytToteutusOid` that overlaps with English-only toteutukset OIDs. Since we don't have the resolved English implementations at this point in upsertProgram (the filtering happens after upsert), instead: filter hakukohteet where the hakukohde itself has an English name (nimi.en exists). This mirrors the toteutukset English filter pattern.
       - For each hakukohde, extract and resolve:
         - `oid`: raw string
         - `name`: resolveLang(h.nimi)
         - `applicationPeriod`: from h.hakuaika — an object `{ start: h.hakuaika?.alkaa ?? null, end: h.hakuaika?.paattyy ?? null }`
         - `requiredEducation`: from first pohjakoulutusvaatimus — `resolveLang(h.pohjakoulutusvaatimukset?.[0]?.nimi) || null`
         - `admissionCriteriaOid`: h.valintapere?.oid ?? null
         - `applicationFormUrl`: find first link with tyyppi "hakulomake" — `h.linkit?.find((l: any) => l.tyyppi === 'hakulomake')?.href ?? null`
         - `implementationOids`: h.kaytetytToteutusOid ?? []
       - Return the filtered/resolved array, or Prisma.JsonNull if nothing passes the filter

    3. In upsertProgram(), after the `const teachingLanguages` line (line 269), extract:
       - `const duration: string | null = detail.kesto ?? null;`
       - `const resolvedHakukohteet = this.resolveHakukohteet(detail.hakukohteet, resolveLang);`

    4. Wire into the upsert create block (after line 297):
       - `hakukohteet: resolvedHakukohteet,`
       - `duration,`

    5. Wire into the upsert update block (after line 318, mirroring implementations):
       - `hakukohteet: this.resolveHakukohteet(detail.hakukohteet, resolveLang),`
       - `duration,`

    6. In src/modules/sync/sync.spec.ts, add a new describe block `resolveHakukohteet()`:
       - Test that null/undefined hakukohteet returns Prisma.JsonNull (use `expect(JSON.stringify(result)).toBe(JSON.stringify(Prisma.JsonNull))` or check result === Prisma.JsonNull)
       - Test that empty array returns Prisma.JsonNull
       - Test that hakukohteet with English names are resolved correctly — mock a hakukohde with nimi.en, hakuaika, pohjakoulutusvaatimukset, valintapere, linkit, kaytetytToteutusOid
       - Test that hakukohteet without English names are filtered out (returns Prisma.JsonNull)
       - Test the full shape: oid, name, applicationPeriod, requiredEducation, admissionCriteriaOid, applicationFormUrl, implementationOids
  </action>
  <verify>
    npm run test -- --testPathPattern="sync" 2>&1
  </verify>
  <done>
    - Program model has hakukohteet (Json?) and duration (String?) fields
    - sync.service.ts has resolveHakukohteet() method with English-language filtering
    - upsertProgram() stores hakukohteet and duration in create and update blocks
    - Sync unit tests pass, covering resolution, filtering, and null handling
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose hakukohteet and duration in program detail response</name>
  <files>
    src/modules/programs/programs.service.ts
    src/modules/programs/programs.spec.ts
  </files>
  <action>
    1. In src/modules/programs/programs.service.ts, in `mapProgramDetail()` (after line 130, near the `implementations` line), add two new fields to the returned object:
       - `hakukohteet: row.hakukohteet ?? null,`
       - `duration: row.duration ?? null,`
       - Order: place them immediately after the `implementations` entry (between implementations and hasGuidance).

    2. In src/modules/programs/programs.spec.ts:
       - Add `hakukohteet` and `duration` to `mockProgramDetailRow`:
         ```typescript
         hakukohteet: [
           {
             oid: 'hakukohde-oid-1',
             name: 'Application Group 1',
             applicationPeriod: { start: '2025-01-01T00:00:00Z', end: '2025-03-15T23:59:59Z' },
             requiredEducation: 'General upper secondary school',
             admissionCriteriaOid: 'valintaperuste-oid-1',
             applicationFormUrl: 'https://opintopolku.fi/app/hakulomake/hakukohde-oid-1',
             implementationOids: ['impl-oid'],
           },
         ],
         duration: '3 years',
         ```
       - Update the existing `findOne` test assertion block (around lines 110-123) to also verify:
         - `expect(result.hakukohteet).toEqual(mockProgramDetailRow.hakukohteet)`
         - `expect(result.duration).toBe('3 years')`
       - Add a test case: `returns null hakukohteet and duration when not present in DB`:
         - Create a row without hakukohteet and duration fields
         - Assert `result.hakukohteet` is null and `result.duration` is null
         - This guards against the existing-DB-rows scenario (null fields should not crash)
  </action>
  <verify>
    npm run test -- --testPathPattern="programs" 2>&1
  </verify>
  <done>
    - mapProgramDetail() returns hakukohteet and duration fields
    - Existing detail response is unchanged except for the two new fields
    - Programs unit tests pass, covering both present and absent scenarios
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Opintopolku upstream API -> SyncService | External data source; hakukohteet data is trusted after validation by resolveHakukohteet |
| SyncService -> PostgreSQL | Application-layer writes; data passed through resolveLang/resolveHakukohteet sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lz4-01 | Tampering | resolveHakukohteet() | mitigate | Type guard at entry: `if (!Array.isArray(hakukohteet))` returns JsonNull. Only known fields extracted; upstream adding unexpected fields cannot leak into DB. |
| T-lz4-02 | Information Disclosure | API response | accept | Hakukohteet data is already public on Opintopolku. Same data, same exposure. No PII. |
</threat_model>

<verification>
1. Unit tests pass for both sync and programs modules
2. `npx prisma validate` confirms schema is valid
3. `npm run build` compiles without errors
</verification>

<success_criteria>
1. `prisma/schema.prisma` includes `hakukohteet Json?` and `duration String?` on the Program model
2. `src/modules/sync/sync.service.ts` extracts hakukohteet from upstream detail, English-resolves text fields, and upserts with the rest of the program data
3. `src/modules/programs/programs.service.ts` returns `hakukohteet` and `duration` in `GET /programs/:oid` detail responses
4. All unit tests pass: `npm run test` exits with 0
5. Existing programs in DB (null fields) serve detail without errors — verified by test case
6. `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/quick/lz4/260502-lz4-SUMMARY.md`
</output>
