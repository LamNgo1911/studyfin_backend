---
phase: 04-user-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
autonomous: true
requirements:
  - FOUND-02
must_haves:
  truths:
    - "User model in schema.prisma has hasTestAccess Boolean @default(false)"
    - "npx prisma db push applied the column to the live database"
    - "Prisma client regenerated and hasTestAccess field accessible on User type"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "User model with hasTestAccess field"
      contains: "hasTestAccess Boolean @default(false)"
  key_links:
    - from: "prisma/schema.prisma"
      to: "generated/prisma"
      via: "npx prisma db push (regenerates client)"
      pattern: "hasTestAccess"
---

<objective>
Add the `hasTestAccess Boolean @default(false)` field to the User model in schema.prisma, then push the schema change to the database and regenerate the Prisma client.

Purpose: FOUND-02 — the hasTestAccess field is the foundation for mock test access monetization gating. Every other Phase 4 task that reads or writes this field depends on this schema change being present and pushed.

Output: Updated schema.prisma with User.hasTestAccess field; Prisma client regenerated with the new field accessible as a typed property.
</objective>

<execution_context>
@/home/liam/Downloads/github_repo/studyfin-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/liam/Downloads/github_repo/studyfin-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/liam/Downloads/github_repo/studyfin-backend/.planning/PROJECT.md
@/home/liam/Downloads/github_repo/studyfin-backend/.planning/ROADMAP.md
@/home/liam/Downloads/github_repo/studyfin-backend/.planning/STATE.md
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Schema push to production-like DB while other connections active could corrupt in-flight queries | MED | `npx prisma db push` is additive (new column with default); no destructive migration; risk confined to dev DB |
| Default value `false` means existing users start without access — intentional, correct for gating | LOW | Accept: conservative default is the intended behavior per D-08; no data loss |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add hasTestAccess field to User model in schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma — read the full file to find exact location of User model fields and confirm current field list before editing
  </read_first>
  <action>
    In prisma/schema.prisma, locate the User model. The current field order (from line ~113) is:

    ```
    model User {
      id              String    @id @default(cuid())
      email           String    @unique
      passwordHash    String
      firstName       String?
      lastName        String?
      role            Role      @default(USER)
      emailVerifiedAt DateTime?
      createdAt       DateTime  @default(now())
      updatedAt       DateTime  @updatedAt
      ...
    ```

    Add `hasTestAccess Boolean  @default(false)` on a new line immediately AFTER the `role` field and BEFORE `emailVerifiedAt`. The resulting field block must look exactly like this:

    ```prisma
      role            Role      @default(USER)
      hasTestAccess   Boolean   @default(false)
      emailVerifiedAt DateTime?
    ```

    Do not modify any other models, enums, relations, or indexes. Do not change field ordering elsewhere.
  </action>
  <verify>
    <automated>grep -n "hasTestAccess" /home/liam/Downloads/github_repo/studyfin-backend/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - grep finds exactly one match: `hasTestAccess   Boolean   @default(false)` (or equivalent whitespace) inside the User model
    - The line appears between the `role` line and the `emailVerifiedAt` line
    - No other model definitions in the file were modified
  </acceptance_criteria>
  <done>schema.prisma User model contains hasTestAccess Boolean field with @default(false)</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema to database and regenerate Prisma client</name>
  <files>generated/prisma</files>
  <read_first>
    - prisma/schema.prisma — confirm hasTestAccess field is present from Task 1 before pushing
  </read_first>
  <action>
    Run the following command from the project root to push the schema change to the database and regenerate the Prisma client:

    ```bash
    npx prisma db push
    ```

    If the command prompts interactively and cannot proceed non-interactively, use:

    ```bash
    npx prisma db push --accept-data-loss
    ```

    The push adds a new column `hasTestAccess BOOLEAN NOT NULL DEFAULT false` to the `User` table. This is non-destructive — existing rows get the default value automatically. After the push, Prisma also regenerates the client in `generated/prisma/`.

    Verify the client was regenerated by checking that `hasTestAccess` appears in the generated type definitions.
  </action>
  <verify>
    <automated>grep -r "hasTestAccess" /home/liam/Downloads/github_repo/studyfin-backend/generated/prisma/ | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` completes without error
    - grep finds `hasTestAccess` in at least one file under `generated/prisma/` (e.g., in the `index.d.ts` type definitions)
    - The generated User type includes `hasTestAccess: boolean`
  </acceptance_criteria>
  <done>Database has the hasTestAccess column; generated Prisma client exports hasTestAccess as a typed boolean field on User</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -n "hasTestAccess" prisma/schema.prisma` returns a line inside the User model
- `grep -r "hasTestAccess" generated/prisma/ | wc -l` returns a number > 0
- `npm run build` exits 0 (TypeScript compilation succeeds with new field)
</verification>

<success_criteria>
FOUND-02 satisfied: User model has `hasTestAccess Boolean @default(false)` in schema.prisma, the column exists in the live PostgreSQL database, and the Prisma client typed User interface includes `hasTestAccess: boolean`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-features/04-01-SUMMARY.md` following the summary template.
</output>
