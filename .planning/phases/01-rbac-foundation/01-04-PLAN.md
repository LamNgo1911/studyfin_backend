---
phase: 01-rbac-foundation
plan: 04
type: execute
wave: 1
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [FOUND-02, FOUND-07]

must_haves:
  truths:
    - "ROADMAP.md success criteria #2 and #3 use @Roles('ADMIN') (uppercase), matching the Prisma enum and implementation"
    - "REQUIREMENTS.md GUID-04 and GUID-05 use @Roles('ADMIN') (uppercase), matching the Prisma enum and implementation"
    - "FOUND-02 is assigned to Phase 4 in both ROADMAP.md requirements list and REQUIREMENTS.md traceability table"
    - "The programs table contains no programs where 'en' is absent from teachingLanguages (confirmed by running cleanup:programs)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected @Roles casing in Phase 1 SC #2 and #3; FOUND-02 removed from Phase 1, added to Phase 4 requirements"
      contains: "@Roles('ADMIN')"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected @Roles casing in GUID-04 and GUID-05; FOUND-02 traceability row updated to Phase 4"
      contains: "@Roles('ADMIN')"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement IDs (FOUND-02, GUID-04, GUID-05)"
      pattern: "FOUND-02.*Phase 4"
---

<objective>
Close the three verification gaps blocking Phase 1 goal achievement.

Purpose: Phase 1 execution delivered correct code but left two documentation inconsistencies (role casing mismatch, orphaned requirement) and one unexecuted operational step (database cleanup). These gaps will cause Phase 3 admin routes to silently deny admin users if left unresolved. FOUND-02 being assigned to Phase 1 in the docs while deferred to Phase 4 in context creates requirement tracking confusion.

Output:
- ROADMAP.md updated: @Roles('ADMIN') casing in SC #2 and #3; FOUND-02 moved to Phase 4 requirements list
- REQUIREMENTS.md updated: @Roles('ADMIN') casing in GUID-04 and GUID-05; FOUND-02 traceability row set to Phase 4
- Database cleanup script executed; output documented in SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rbac-foundation/01-VERIFICATION.md
@.planning/phases/01-rbac-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix role casing and FOUND-02 phase assignment in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — read entire file before editing; identify lines 29, 30, 26, and the Phase 4 requirements line
  </read_first>
  <action>
Make exactly three targeted edits to .planning/ROADMAP.md:

Edit 1 — Fix SC #2 (line 29):
  Before: `  2. A route decorated with \`@Roles('admin')\` returns 403 when called by a user with role "user"`
  After:  `  2. A route decorated with \`@Roles('ADMIN')\` returns 403 when called by a user with role "USER"`

Edit 2 — Fix SC #3 (line 30):
  Before: `  3. A route decorated with \`@Roles('admin')\` returns 200 when called by a user with role "admin"`
  After:  `  3. A route decorated with \`@Roles('ADMIN')\` returns 200 when called by a user with role "ADMIN"`

Edit 3 — Reassign FOUND-02 from Phase 1 to Phase 4:
  Phase 1 requirements line (line 26):
    Before: `**Requirements**: FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, FOUND-07`
    After:  `**Requirements**: FOUND-01, FOUND-03, FOUND-04, FOUND-05, FOUND-06, FOUND-07`

  Phase 4 requirements line (line 65):
    Before: `**Requirements**: USER-01, USER-02, USER-03, USER-04, USER-05, USER-06, USER-07, USER-08, USER-09`
    After:  `**Requirements**: FOUND-02, USER-01, USER-02, USER-03, USER-04, USER-05, USER-06, USER-07, USER-08, USER-09`

Do not change any other content — no rewording, no reformatting, no other line changes.
  </action>
  <verify>
    grep -n "@Roles('ADMIN')" .planning/ROADMAP.md
    # Must return lines 29 and 30 with @Roles('ADMIN') (uppercase)

    grep -n "FOUND-02" .planning/ROADMAP.md
    # Must return only the Phase 4 requirements line — NOT the Phase 1 requirements line
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md line 29 contains `@Roles('ADMIN')` (uppercase A) and role "USER" (uppercase)
    - .planning/ROADMAP.md line 30 contains `@Roles('ADMIN')` (uppercase A) and role "ADMIN" (uppercase)
    - grep "FOUND-02" .planning/ROADMAP.md returns exactly one match, on the Phase 4 Requirements line
    - grep "FOUND-02" .planning/ROADMAP.md does NOT return any match on the Phase 1 Requirements line
    - grep "FOUND-01.*FOUND-02" .planning/ROADMAP.md returns no matches (FOUND-02 no longer adjacent to FOUND-01 in Phase 1)
    - grep "FOUND-02.*USER-01" .planning/ROADMAP.md returns one match (FOUND-02 precedes USER-01 in Phase 4)
  </acceptance_criteria>
  <done>ROADMAP.md has correct uppercase @Roles('ADMIN') in SC #2 and #3, and FOUND-02 is listed only under Phase 4 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Fix role casing and FOUND-02 phase assignment in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read entire file before editing; identify lines 38, 39, 118
  </read_first>
  <action>
Make exactly three targeted edits to .planning/REQUIREMENTS.md:

Edit 1 — Fix GUID-04 (line 38):
  Before: `- [ ] **GUID-04**: \`GuidanceModule\` has admin \`POST /guidance/:programOid\` guarded by \`@UseGuards(JwtAuthGuard, RolesGuard) @Roles('admin')\``
  After:  `- [ ] **GUID-04**: \`GuidanceModule\` has admin \`POST /guidance/:programOid\` guarded by \`@UseGuards(JwtAuthGuard, RolesGuard) @Roles('ADMIN')\``

Edit 2 — Fix GUID-05 (line 39):
  Before: `- [ ] **GUID-05**: \`GuidanceModule\` has admin \`PATCH /guidance/:programOid\` guarded by \`@UseGuards(JwtAuthGuard, RolesGuard) @Roles('admin')\``
  After:  `- [ ] **GUID-05**: \`GuidanceModule\` has admin \`PATCH /guidance/:programOid\` guarded by \`@UseGuards(JwtAuthGuard, RolesGuard) @Roles('ADMIN')\``

Edit 3 — Reassign FOUND-02 traceability row (line 118):
  Before: `| FOUND-02 | Phase 1 | Pending |`
  After:  `| FOUND-02 | Phase 4 | Pending |`

Do not change any other content — no rewording, no reformatting, no other line changes.
  </action>
  <verify>
    grep -n "@Roles('ADMIN')" .planning/REQUIREMENTS.md
    # Must return lines for GUID-04 and GUID-05 with @Roles('ADMIN') (uppercase)

    grep -n "FOUND-02" .planning/REQUIREMENTS.md
    # Must return exactly two lines: the requirement definition line and the traceability row showing "Phase 4"
  </verify>
  <acceptance_criteria>
    - grep "@Roles('ADMIN')" .planning/REQUIREMENTS.md returns exactly two matches (GUID-04 and GUID-05 lines)
    - grep "@Roles('admin')" .planning/REQUIREMENTS.md returns zero matches (no lowercase instances remain in GUID-04/GUID-05)
    - grep "FOUND-02 | Phase 1" .planning/REQUIREMENTS.md returns zero matches
    - grep "FOUND-02 | Phase 4" .planning/REQUIREMENTS.md returns exactly one match (traceability table row)
  </acceptance_criteria>
  <done>REQUIREMENTS.md has correct uppercase @Roles('ADMIN') in GUID-04 and GUID-05, and the FOUND-02 traceability row shows Phase 4.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run cleanup:programs against live database and document output</name>
  <what-built>
    prisma/cleanup-non-english.ts — bulk delete of programs where 'en' is not in teachingLanguages. Script is correct and ready (verified in 01-03). This step requires a live database connection; Claude cannot execute it without a running DB.
  </what-built>
  <how-to-verify>
    1. Ensure DATABASE_URL is set in your environment pointing to a populated database.
    2. Run the cleanup script:
       ```bash
       npm run cleanup:programs
       ```
    3. The script logs one of two messages:
       - "Deleted N non-English programs" where N >= 0, then exits 0
       - An error message if the DB is unreachable, then exits 1
    4. After a successful run, verify the database state:
       ```sql
       SELECT COUNT(*) FROM "Program" WHERE NOT ('en' = ANY("teachingLanguages"));
       ```
       Expected result: 0 rows.
    5. Note the number of deleted programs (N) from the script output. You will provide it in the resume signal below.

    If N = 0: no non-English programs existed — DB was already clean. SC #4 is satisfied.
    If N > 0: programs were deleted — SC #4 is now satisfied.
  </how-to-verify>
  <resume-signal>
    Type "done: N programs deleted" (replace N with the count from the script output), or "done: 0 programs deleted" if the DB was already clean, or "skipped: no DB available" if you cannot run it now.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> database | cleanup:programs script opens a direct DB connection; DATABASE_URL must be a trusted connection string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | prisma/cleanup-non-english.ts | accept | Script is read-only from a trust perspective — it only deletes rows matching a deterministic filter. The filter is `NOT { teachingLanguages: { has: 'en' } }`. No user input accepted. Operator reviews before running. |
| T-01-11 | Tampering | .planning/ROADMAP.md, .planning/REQUIREMENTS.md | accept | Planning docs are developer-only artifacts in the git repo. No runtime trust boundary. Changes are code-reviewed via git history. |
</threat_model>

<verification>
After all three tasks complete:

1. Role casing consistent:
   ```bash
   grep "@Roles('admin')" .planning/ROADMAP.md .planning/REQUIREMENTS.md
   # Must return zero matches
   grep "@Roles('ADMIN')" .planning/ROADMAP.md .planning/REQUIREMENTS.md
   # Must return four matches: SC #2, SC #3, GUID-04, GUID-05
   ```

2. FOUND-02 correctly assigned to Phase 4:
   ```bash
   grep "FOUND-02" .planning/ROADMAP.md
   # Returns one match — Phase 4 requirements line only
   grep "FOUND-02" .planning/REQUIREMENTS.md
   # Returns two matches — requirement definition (unchanged) and traceability row showing "Phase 4"
   ```

3. Database cleanup executed (confirmed by human in Task 3 checkpoint).
</verification>

<success_criteria>
- Zero occurrences of @Roles('admin') (lowercase) in ROADMAP.md and REQUIREMENTS.md
- Four occurrences of @Roles('ADMIN') (uppercase) in ROADMAP.md and REQUIREMENTS.md (SC #2, SC #3, GUID-04, GUID-05)
- FOUND-02 listed in Phase 4 requirements in ROADMAP.md (not Phase 1)
- FOUND-02 traceability row in REQUIREMENTS.md shows "Phase 4"
- cleanup:programs script has been run and operator has documented the deleted-count output
- All three verification gaps from 01-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-rbac-foundation/01-04-SUMMARY.md`

Include in the summary:
- Which gaps were closed (Gap 1, Gap 2, Gap 3 from 01-VERIFICATION.md)
- Exact before/after for each changed line in ROADMAP.md and REQUIREMENTS.md
- The deleted program count from the cleanup:programs run (from Task 3 checkpoint)
- Confirmation that zero occurrences of @Roles('admin') remain in planning docs
</output>
