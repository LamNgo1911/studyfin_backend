---
phase: 05-search-and-infrastructure
plan: 05
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/modules/auth/dto/register.dto.ts
  - src/modules/auth/dto/login.dto.ts
  - src/modules/auth/dto/refresh-token.dto.ts
  - src/modules/auth/dto/forgot-password.dto.ts
  - src/modules/auth/dto/reset-password.dto.ts
  - src/modules/auth/dto/verify-email.dto.ts
  - src/modules/guidance/dto/create-guidance.dto.ts
  - src/modules/guidance/dto/update-guidance.dto.ts
  - src/modules/guidance/dto/guidance-section.dto.ts
  - src/modules/users/dto/update-profile.dto.ts
  - src/modules/users/dto/list-saved-programs-query.dto.ts
  - src/modules/users/dto/update-saved-program.dto.ts
autonomous: true
requirements:
  - SRCH-08
user_setup: []
must_haves:
  truths:
    - "Auth DTOs (RegisterDto, LoginDto, RefreshTokenDto, ForgotPasswordDto, ResetPasswordDto, VerifyEmailDto) have @ApiProperty on all fields"
    - "Guidance DTOs (CreateGuidanceDto, UpdateGuidanceDto, GuidanceSectionDto) have @ApiProperty on all fields"
    - "Users DTOs (UpdateProfileDto, ListSavedProgramsQueryDto, UpdateSavedProgramDto) have @ApiProperty on all fields"
  artifacts:
    - path: src/modules/auth/dto/register.dto.ts
      provides: "RegisterDto with @ApiProperty on email and password"
      contains: "@ApiProperty"
    - path: src/modules/guidance/dto/guidance-section.dto.ts
      provides: "GuidanceSectionDto with @ApiProperty on all 4 fields (key, title, body, order)"
      contains: "@ApiProperty"
  key_links:
    - from: all DTOs
      to: "@nestjs/swagger"
      via: "@ApiProperty / @ApiPropertyOptional imports"
---

<objective>
Add @ApiProperty and @ApiPropertyOptional decorators to all public input DTOs per D-08. This is a split from the original Plan 04 to keep file count manageable (12 DTO files, each receiving 1-4 lines of decorators).

Purpose: Swagger UI at /api shows field-level documentation for all request body and query DTOs. Response shapes are inferred from TypeScript return types per D-08.

Output: 12 DTO files get @ApiProperty / @ApiPropertyOptional decorators on every field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

Per D-08: Input DTOs only -- @ApiProperty on request body and query DTOs. Response shapes inferred from TypeScript return types. No @ApiResponse decorators on controllers.

Plan 04 (parallel to this plan in Wave 2) adds @ApiTags and @Throttle to controllers. This plan only handles DTO field decorators. Both can run in parallel since they modify disjoint files.

The pattern:
- Required fields: `@ApiProperty({ description: '...' })`
- Optional fields: `@ApiPropertyOptional({ description: '...' })`
- Fields with defaults include `default:` in the decorator
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @ApiProperty / @ApiPropertyOptional decorators to all 12 public input DTOs</name>
  <files>
    src/modules/auth/dto/register.dto.ts,
    src/modules/auth/dto/login.dto.ts,
    src/modules/auth/dto/refresh-token.dto.ts,
    src/modules/auth/dto/forgot-password.dto.ts,
    src/modules/auth/dto/reset-password.dto.ts,
    src/modules/auth/dto/verify-email.dto.ts,
    src/modules/guidance/dto/create-guidance.dto.ts,
    src/modules/guidance/dto/update-guidance.dto.ts,
    src/modules/guidance/dto/guidance-section.dto.ts,
    src/modules/users/dto/update-profile.dto.ts,
    src/modules/users/dto/list-saved-programs-query.dto.ts,
    src/modules/users/dto/update-saved-program.dto.ts
  </files>
  <read_first>
    src/modules/auth/dto/register.dto.ts,
    src/modules/auth/dto/login.dto.ts,
    src/modules/auth/dto/refresh-token.dto.ts,
    src/modules/auth/dto/forgot-password.dto.ts,
    src/modules/auth/dto/reset-password.dto.ts,
    src/modules/auth/dto/verify-email.dto.ts,
    src/modules/guidance/dto/create-guidance.dto.ts,
    src/modules/guidance/dto/update-guidance.dto.ts,
    src/modules/guidance/dto/guidance-section.dto.ts,
    src/modules/users/dto/update-profile.dto.ts,
    src/modules/users/dto/list-saved-programs-query.dto.ts,
    src/modules/users/dto/update-saved-program.dto.ts
  </read_first>
  <action>
    Add `@ApiProperty` / `@ApiPropertyOptional` decorators to every field in each DTO. Import from `@nestjs/swagger`.

    **1. src/modules/auth/dto/register.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    Decorate each field:
    - `email` -> `@ApiProperty({ description: 'User email address' })`
    - `password` -> `@ApiProperty({ description: 'Password (min 8 chars)' })`

    **2. src/modules/auth/dto/login.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `email` -> `@ApiProperty({ description: 'User email address' })`
    - `password` -> `@ApiProperty({ description: 'User password' })`

    **3. src/modules/auth/dto/refresh-token.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `refreshToken` -> `@ApiProperty({ description: 'JWT refresh token' })`

    **4. src/modules/auth/dto/forgot-password.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `email` -> `@ApiProperty({ description: 'Registered email address' })`

    **5. src/modules/auth/dto/reset-password.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `token` -> `@ApiProperty({ description: 'Password reset token from email' })`
    - `newPassword` -> `@ApiProperty({ description: 'New password (min 8 chars)' })`

    **6. src/modules/auth/dto/verify-email.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `token` -> `@ApiProperty({ description: 'Email verification token' })`

    **7. src/modules/guidance/dto/create-guidance.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `sections` -> `@ApiProperty({ type: [GuidanceSectionDto], description: 'Guidance sections to create/replace' })`

    **8. src/modules/guidance/dto/update-guidance.dto.ts**
    Add import:
    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    ```
    - `sections` -> `@ApiPropertyOptional({ type: [GuidanceSectionDto], description: 'Sections to upsert' })`
    - `deleteKeys` -> `@ApiPropertyOptional({ type: [String], description: 'Section keys to delete' })`

    **9. src/modules/guidance/dto/guidance-section.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
    ```
    - `key` -> `@ApiProperty({ description: 'Unique section key (e.g. application, visa, housing)' })`
    - `title` -> `@ApiProperty({ description: 'Section title' })`
    - `body` -> `@ApiProperty({ description: 'Section body content (markdown)' })`
    - `order` -> `@ApiProperty({ description: 'Display order (ascending)' })`

    **10. src/modules/users/dto/update-profile.dto.ts**
    Add import:
    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    ```
    - `firstName` -> `@ApiPropertyOptional({ description: 'User first name' })`
    - `lastName` -> `@ApiPropertyOptional({ description: 'User last name' })`

    **11. src/modules/users/dto/list-saved-programs-query.dto.ts**
    Add import:
    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    ```
    - `page` -> `@ApiPropertyOptional({ default: 0, description: 'Page number (0-indexed)' })`
    - `size` -> `@ApiPropertyOptional({ default: 20, description: 'Results per page' })`
    - `status` -> `@ApiPropertyOptional({ description: 'Filter by program status' })`

    **12. src/modules/users/dto/update-saved-program.dto.ts**
    Add import:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    ```
    - `status` -> `@ApiProperty({ description: 'New status for the saved program' })`

    After all files updated, verify:
    ```bash
    npm run build 2>&1 | tail -10
    ```
  </action>
  <verify>
    <automated>
      grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/auth/dto/register.dto.ts | grep -q 1 && grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/auth/dto/login.dto.ts | grep -q 1 && grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/auth/dto/refresh-token.dto.ts | grep -q 1 && grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/auth/dto/forgot-password.dto.ts | grep -q 1 && grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/auth/dto/reset-password.dto.ts | grep -q 1 && grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/auth/dto/verify-email.dto.ts | grep -q 1 && grep -c '@ApiProperty\|@ApiPropertyOptional' src/modules/guidance/dto/guidance-section.dto.ts | grep -q 4 && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - All auth DTO files (register, login, refresh, forgot, reset, verify) have @ApiProperty on every field
    - GuidanceSectionDto has @ApiProperty on key, title, body, order fields
    - CreateGuidanceDto has @ApiProperty on sections field
    - UpdateGuidanceDto has @ApiPropertyOptional on sections and deleteKeys fields
    - UpdateProfileDto has @ApiPropertyOptional on firstName, lastName
    - ListSavedProgramsQueryDto has @ApiPropertyOptional on page, size, status
    - UpdateSavedProgramDto has @ApiProperty on status field
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    All 12 public input DTO files have @ApiProperty/@ApiPropertyOptional on every field. Build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Decorators have no runtime effect on trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | @ApiProperty on DTOs | accept | Decorators expose field descriptions and types in Swagger UI but contain no runtime secrets, credentials, or internal logic. Acceptable for a public API. |
</threat_model>

<verification>

### Multi-Source Audit

| Source Item | Covered By | Status |
|-------------|------------|--------|
| SRCH-08: @nestjs/swagger + /api UI | Plan 1 (Swagger bootstrap) + Plan 4 (controller tags) + This plan (DTO decorators) | Covered |
| D-08: Input DTOs only, no @ApiResponse on controllers | This plan (all @ApiProperty on DTOs) | Covered |

### Plan-Level Checks

1. Each DTO file has @ApiProperty or @ApiPropertyOptional on every field
2. npm run build succeeds
3. npm run test passes (no behavioral changes expected from decorators)
</verification>

<success_criteria>
1. npm run build exits 0
2. npm run test passes (zero behavioral changes -- decorators only)
3. Start the app with `npm run start:dev`, visit `http://localhost:3000/api` -- Swagger UI shows field-level documentation for each DTO
4. All 12 DTO files have @ApiProperty/@ApiPropertyOptional on every field
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-and-infrastructure/05-05-SUMMARY.md`
</output>
